import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;

  padding: 2rem 0 0 0;

  width: 100%;

  p {
    font-weight: 600;

    margin-top: 1rem;

    opacity: 0.1;

    position: absolute;

    font-size: 3rem;
  }

  h1 {
    margin-top: 1rem;

    color: var(--green);

    position: relative;
  }
`;
